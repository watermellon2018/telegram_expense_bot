# 📊 Детальное логирование - Руководство

## Обзор

Проект использует **структурированное логирование** с детальной информацией о каждой операции, сообщении и ошибке.

## Обязательные поля

Каждая запись лога содержит:
- **timestamp** - время события (ISO 8601, UTC)
- **level** - уровень (INFO, ERROR, WARNING, DEBUG)
- **service** - название модуля/сервиса
- **event** - название события

## Дополнительные поля (по контексту)

### Пользовательские операции
- `user_id` - ID пользователя Telegram
- `username` - username пользователя
- `chat_id` - ID чата
- `chat_type` - тип чата (private, group, etc.)

### Команды и сообщения
- `command` - название команды (/add, /export, etc.)
- `is_command` - является ли сообщение командой
- `text_preview` - первые 100 символов текста
- `text_length` - длина сообщения
- `message_id` - ID сообщения
- `callback_data` - данные callback query

### Операции с данными
- `project_id` - ID проекта
- `amount` - сумма расхода
- `category` - категория расхода
- `action` - выполняемое действие
- `status` - статус операции (success, failed, error)

### Операции с БД
- `table` - название таблицы
- `operation` - тип операции (SELECT, INSERT, UPDATE, DELETE)
- `query` - SQL запрос (первые 80-100 символов)
- `rows_returned` - количество возвращенных строк
- `duration` - длительность операции (секунды)

### Ошибки
- `error` - текст ошибки
- `error_type` - тип исключения
- `reason` - причина ошибки/неудачи

### Файлы и экспорт
- `filename` - имя файла
- `file_size` - размер файла
- `export_type` - тип экспорта (all, year, month)

## Примеры логов

### Входящее сообщение
```
[2026-01-16T18:49:45.926Z] INFO [telegram.updates] message_received 
  user_id=400564356 username=testuser chat_id=400564356 
  is_command=true command=/add text_length=25 
  text_preview="/add 100 продукты хлеб"
```

### Команда выполнена
```
[2026-01-16T18:49:45.940Z] INFO [handlers.expense] command_executed 
  user_id=400564356 command=add project_id=1
```

### Добавление расхода
```
[2026-01-16T18:49:45.949Z] INFO [utils.excel] add_expense_start 
  user_id=400564356 project_id=1 amount=100.0 category=продукты

[2026-01-16T18:49:45.951Z] INFO [utils.excel] add_expense_success 
  user_id=400564356 project_id=1 amount=100.0 category=продукты 
  duration=0.002

[2026-01-16T18:49:45.952Z] INFO [handlers.expense] expense_added 
  user_id=400564356 project_id=1 amount=100.0 category=продукты 
  has_description=true
```

### Операция с БД
```
[2026-01-16T18:49:45.950Z] INFO [utils.db] database_operation 
  action=INSERT table=expenses duration=0.001 
  query="INSERT INTO expenses(user_id, project_id, date, time, amount..."
```

### Ошибка
```
[2026-01-16T18:49:45.960Z] ERROR [handlers.expense] error_occurred 
  user_id=400564356 error="Invalid category" error_type=ValueError 
  action=add_expense category=неправильная_категория
```

### Валидация
```
[2026-01-16T18:49:45.955Z] INFO [handlers.expense] invalid_amount_format 
  user_id=400564356 input_text="abc" reason="not_a_number"

[2026-01-16T18:49:45.956Z] INFO [handlers.expense] invalid_category_selected 
  user_id=400564356 category=неправильная amount=100.0 
  reason="category_not_in_list"
```

### Отсутствие данных
```
[2026-01-16T18:49:45.970Z] INFO [handlers.stats] month_stats_empty 
  user_id=400564356 project_id=1 month=1 year=2026

[2026-01-16T18:49:45.975Z] INFO [utils.excel] get_month_expenses_empty 
  user_id=400564356 month=1 year=2026 project_id=1
```

## События по категориям

### Telegram Updates
- `message_received` - получено текстовое сообщение
- `callback_received` - получен callback query
- `update_received` - получено другое обновление

### Команды
- `command_executed` - команда выполнена
- `command_start` - начало выполнения команды
- `command_success` - команда выполнена успешно
- `command_error` - ошибка при выполнении команды

### Расходы
- `expense_added` - расход добавлен
- `expense_parsed_from_text` - расход распознан из текста
- `expense_add_failed` - ошибка добавления расхода
- `add_expense_start` - начало добавления расхода
- `add_expense_success` - расход успешно добавлен
- `add_expense_error` - ошибка при добавлении

### Валидация
- `invalid_amount_format` - неверный формат суммы
- `invalid_amount` - невалидная сумма (<= 0)
- `invalid_category_selected` - неверная категория
- `invalid_category_in_text` - неверная категория в тексте
- `invalid_command_format` - неверный формат команды
- `amount_validated` - сумма валидирована
- `category_validated` - категория валидирована

### Проекты
- `project_create_start` - начало создания проекта
- `project_create_success` - проект создан
- `project_create_failed` - проект не создан
- `project_list_empty` - список проектов пуст
- `project_list_success` - список проектов получен
- `project_select_success` - проект выбран
- `project_delete_success` - проект удален

### Статистика
- `month_stats_requested` - запрошена статистика за месяц
- `month_stats_sent` - статистика отправлена
- `month_stats_empty` - нет данных за месяц
- `month_chart_sent` - график отправлен
- `month_chart_failed` - ошибка создания графика

### Экспорт
- `export_start` - начало экспорта
- `export_success` - экспорт успешен
- `export_error` - ошибка экспорта
- `export_no_data` - нет данных для экспорта
- `get_years_success` - список годов получен
- `get_years_empty` - нет доступных годов

### База данных
- `db_pool_init_start` - начало инициализации пула
- `db_pool_init_success` - пул инициализирован
- `db_pool_close_start` - начало закрытия пула
- `db_pool_close_success` - пул закрыт
- `database_operation` - операция с БД
- `db_fetch_error` - ошибка выборки
- `db_execute_error` - ошибка выполнения запроса

### Системные
- `bot_started` - бот запущен
- `bot_shutdown` - бот остановлен
- `bot_stopped` - бот остановлен (прерван)
- `system_initialized` - система инициализирована
- `critical_error` - критическая ошибка
- `telegram_error` - ошибка Telegram API

## Middleware для логирования

Все входящие обновления автоматически логируются через `LoggingHandler` в `main.py`:
- Логирует каждое сообщение с деталями
- Логирует каждый callback query
- Логирует другие типы обновлений

## Обработка ошибок

Все ошибки логируются с полным контекстом:
- Тип исключения
- Текст ошибки
- Контекст операции (user_id, project_id, etc.)
- Длительность операции (если применимо)

## Производительность

Медленные операции автоматически логируются:
- БД операции > 0.01 сек
- SELECT запросы > 0.05 сек
- Экспорт с duration
- Создание графиков с duration

## Фильтрация

Служебные операции фильтруются:
- `INSERT INTO users` - только если медленные
- `INSERT INTO budget` - только если медленные
- `UPDATE budget` - только если медленные

Это уменьшает шум в логах при нормальной работе.

## Настройка

В `config.py`:
```python
LOG_LEVEL = "INFO"  # или DEBUG, WARNING, ERROR
JSON_LOG_FORMAT = False  # True для JSON формата
LOG_FILE = None  # Путь к файлу логов
```

## Использование в коде

```python
from utils.logger import get_logger, log_event, log_error, log_command

logger = get_logger("handlers.my_handler")

# Логирование команды
log_command(logger, "my_command", user_id=user_id, project_id=project_id)

# Логирование события
log_event(logger, "operation_start", user_id=user_id, action="do_something")

# Логирование ошибки
try:
    # код
except Exception as e:
    log_error(logger, e, "operation_failed", user_id=user_id, action="do_something")
```

## Анализ логов

### Поиск проблем пользователя
```bash
grep "user_id=400564356" logs.txt | grep ERROR
```

### Поиск медленных операций
```bash
grep "duration" logs.txt | awk '$NF > 1.0'
```

### Статистика команд
```bash
grep "command_executed" logs.txt | awk '{print $NF}' | sort | uniq -c
```

### Ошибки по типам
```bash
grep "error_type" logs.txt | awk '{print $NF}' | sort | uniq -c
```

## JSON формат

Для production используйте JSON формат:
```bash
export JSON_LOG_FORMAT=true
```

Каждая строка будет валидным JSON объектом, удобным для парсинга и анализа в системах мониторинга.
