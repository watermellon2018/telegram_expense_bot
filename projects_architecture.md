# Архитектура функционала "Проекты"

## Обзор

Функционал "Проекты" позволяет пользователям группировать расходы по отдельным проектам (например, "Отпуск", "Ремонт", "Свадьба"), не смешивая их с общими расходами.

## Структура данных

### 1. Хранение информации о проектах

Для каждого пользователя будет создан файл `projects.xlsx` в директории пользователя:
- Путь: `data/users/{user_id}/projects.xlsx`

Структура файла `projects.xlsx`:

**Лист "Projects":**
- `project_id` - уникальный идентификатор проекта (автоинкремент)
- `project_name` - название проекта
- `created_date` - дата создания проекта
- `is_active` - флаг активности (True/False)

**Лист "ActiveProject":**
- `active_project_id` - ID текущего активного проекта (NULL = общие расходы)

### 2. Хранение расходов проектов

Расходы проектов будут храниться в отдельных Excel-файлах:
- Путь: `data/users/{user_id}/projects/{project_id}/{year}.xlsx`

Структура файлов расходов проектов идентична общим расходам:
- Лист "Expenses" с теми же полями
- Лист "Categories" с категориями
- Лист "Budget" с бюджетом

### 3. Контекст пользователя

В `context.user_data` будет храниться:
- `active_project_id` - ID активного проекта (None = общие расходы)

## Функциональные возможности

### 1. Управление проектами

**Команда `/project_create <название>`**
- Создает новый проект
- Добавляет запись в `projects.xlsx`
- Создает директорию для проекта

**Команда `/project_list`**
- Показывает список всех проектов
- Отображает активный проект
- Показывает общую статистику по каждому проекту

**Команда `/project_select <название или ID>`**
- Переключает на выбранный проект
- Все последующие расходы будут записываться в этот проект
- Обновляет контекст пользователя

**Команда `/project_main`**
- Переключает на общие расходы
- Сбрасывает активный проект

**Команда `/project_delete <название или ID>`**
- Удаляет проект
- Удаляет все расходы проекта
- Запрашивает подтверждение

**Команда `/project_info`**
- Показывает информацию о текущем активном проекте
- Если активны общие расходы, показывает это

### 2. Работа с расходами

Все существующие команды работают с учетом активного проекта:
- `/add` - добавляет расход в активный проект или общие расходы
- Прямой ввод суммы - также учитывает активный проект

### 3. Статистика и экспорт

Все команды статистики работают с учетом активного проекта:
- `/month` - статистика за месяц для активного проекта
- `/stats` - общая статистика для активного проекта
- `/category` - статистика по категории для активного проекта

### 4. Визуальные индикаторы

В каждом ответе бота будет отображаться текущий контекст:
- `📊 Общие расходы` - если активны общие расходы
- `📁 Проект: {название}` - если активен проект

## Изменения в коде

### 1. Новый модуль `utils/projects.py`

Функции для работы с проектами:
- `create_project(user_id, project_name)` - создание проекта
- `get_all_projects(user_id)` - получение списка проектов
- `get_project_by_id(user_id, project_id)` - получение проекта по ID
- `get_project_by_name(user_id, project_name)` - получение проекта по имени
- `delete_project(user_id, project_id)` - удаление проекта
- `set_active_project(user_id, project_id)` - установка активного проекта
- `get_active_project(user_id)` - получение активного проекта
- `get_project_stats(user_id, project_id)` - статистика по проекту

### 2. Новый обработчик `handlers/project.py`

Обработчики команд для работы с проектами:
- `project_create_command()`
- `project_list_command()`
- `project_select_command()`
- `project_main_command()`
- `project_delete_command()`
- `project_info_command()`

### 3. Изменения в `utils/excel.py`

Модификация функций для поддержки проектов:
- `get_excel_path(user_id, year, project_id=None)` - добавить параметр project_id
- `create_excel_file(user_id, year, project_id=None)` - добавить параметр project_id
- `add_expense(user_id, amount, category, description, project_id=None)` - добавить параметр project_id
- Все остальные функции также получают параметр `project_id=None`

### 4. Изменения в `handlers/expense.py`

Модификация обработчиков для учета активного проекта:
- Получать `active_project_id` из `context.user_data`
- Передавать `project_id` в функции `excel.add_expense()`
- Отображать текущий контекст в подтверждениях

### 5. Изменения в `handlers/stats.py`

Модификация обработчиков статистики:
- Получать `active_project_id` из `context.user_data`
- Передавать `project_id` в функции получения статистики
- Отображать текущий контекст в отчетах

### 6. Изменения в `handlers/export.py`

Модификация обработчиков экспорта:
- Получать `active_project_id` из `context.user_data`
- Экспортировать данные активного проекта

### 7. Изменения в `config.py`

Добавить константы:
- `PROJECTS_FILE = "projects.xlsx"` - имя файла с проектами
- `PROJECTS_DIR = "projects"` - имя директории для проектов

## Пользовательский сценарий

1. Пользователь создает проект: `/project_create Отпуск`
2. Бот подтверждает создание и автоматически переключается на проект
3. Пользователь добавляет расходы: `/add 1000 рестораны ужин`
4. Расходы записываются в проект "Отпуск"
5. Пользователь может посмотреть статистику: `/month`
6. Статистика показывается только для проекта "Отпуск"
7. Пользователь переключается на общие расходы: `/project_main`
8. Теперь все расходы идут в общие
9. Пользователь может переключиться обратно: `/project_select Отпуск`
10. После завершения проекта можно удалить: `/project_delete Отпуск`

## Преимущества архитектуры

1. **Изоляция данных**: Расходы проектов хранятся отдельно от общих
2. **Обратная совместимость**: Существующие данные не затрагиваются
3. **Простота использования**: Интуитивные команды
4. **Гибкость**: Легко переключаться между проектами
5. **Масштабируемость**: Можно создавать неограниченное количество проектов
