# Функционал "Проекты"

## Описание

Функционал "Проекты" позволяет пользователям группировать расходы по отдельным проектам, не смешивая их с общими расходами. Это полезно для отслеживания расходов на конкретные цели, такие как отпуск, ремонт, свадьба и т.д.

## Основные возможности

### 1. Создание проекта

Команда: `/project_create <название>`

Создает новый проект с указанным названием и автоматически переключается на него.

**Пример:**
```
/project_create Отпуск
```

**Результат:**
- Создается новый проект "Отпуск"
- Проект автоматически становится активным
- Все последующие расходы будут записываться в этот проект

### 2. Просмотр списка проектов

Команда: `/project_list`

Отображает список всех созданных проектов с их статистикой.

**Информация о каждом проекте:**
- Название проекта
- ID проекта
- Дата создания
- Количество расходов
- Общая сумма расходов
- Отметка активного проекта

### 3. Переключение на проект

Команда: `/project_select <название или ID>`

Переключает на указанный проект. Все последующие расходы будут записываться в выбранный проект.

**Примеры:**
```
/project_select Отпуск
/project_select 1
```

### 4. Переключение на общие расходы

Команда: `/project_main`

Переключает на общие расходы. Все последующие расходы будут записываться в общую базу.

### 5. Удаление проекта

Команда: `/project_delete <название или ID>`

Удаляет проект со всеми его расходами. Требует подтверждения.

**Примеры:**
```
/project_delete Отпуск
/project_delete 1
```

**Важно:** Удаление проекта необратимо и удаляет все связанные с ним расходы!

### 6. Информация о текущем проекте

Команда: `/project_info`

Отображает информацию о текущем активном проекте или сообщает, что активны общие расходы.

**Информация включает:**
- Название проекта
- ID проекта
- Дата создания
- Количество расходов
- Общая сумма расходов
- Распределение по категориям

## Работа с расходами в проектах

Все существующие команды для работы с расходами автоматически учитывают активный проект:

### Добавление расходов

Команды `/add` и прямой ввод суммы работают с учетом активного проекта:

```
/add 1000 рестораны ужин
```

Расход будет добавлен в активный проект (если выбран) или в общие расходы.

### Статистика

Команды статистики показывают данные только для активного проекта:

- `/month` - статистика за месяц для активного проекта
- `/day` - статистика за день для активного проекта
- `/stats` - общая статистика для активного проекта
- `/category <название>` - статистика по категории для активного проекта


### Визуальные индикаторы

В каждом ответе бота отображается текущий контекст:

- `📊 Общие расходы` - если активны общие расходы
- `📁 Проект: Отпуск` - если активен проект "Отпуск"

## Структура хранения данных

### Файл проектов

Для каждого пользователя создается файл `projects.xlsx` в директории пользователя:
- Путь: `data/users/{user_id}/projects.xlsx`

**Лист "Projects":**
- `project_id` - уникальный идентификатор проекта
- `project_name` - название проекта
- `created_date` - дата создания
- `is_active` - флаг активности

**Лист "ActiveProject":**
- `active_project_id` - ID текущего активного проекта

### Файлы расходов проектов

Расходы каждого проекта хранятся в отдельных файлах:
- Путь: `data/users/{user_id}/projects/{project_id}/{year}.xlsx`

Структура файлов идентична общим расходам:
- Лист "Expenses" - записи о расходах
- Лист "Categories" - список категорий
- Лист "Budget" - информация о бюджете

## Типичные сценарии использования

### Сценарий 1: Планирование отпуска

1. Создать проект: `/project_create Отпуск`
2. Добавлять расходы: `/add 5000 транспорт билеты`
3. Смотреть статистику: `/month`
4. После отпуска: `/project_main` (вернуться к общим расходам)

### Сценарий 2: Несколько проектов

1. Создать первый проект: `/project_create Ремонт`
2. Добавить расходы на ремонт
3. Создать второй проект: `/project_create Свадьба`
4. Добавить расходы на свадьбу
5. Переключаться между проектами: `/project_select Ремонт` или `/project_select Свадьба`
6. Смотреть список всех проектов: `/project_list`

### Сценарий 3: Завершение проекта

1. Экспортировать данные: `/export`
2. Удалить проект: `/project_delete Отпуск`
3. Подтвердить удаление

## Технические детали

### Модули

**utils/projects.py** - модуль для работы с проектами:
- `create_project()` - создание проекта
- `get_all_projects()` - получение списка проектов
- `get_project_by_id()` - получение проекта по ID
- `get_project_by_name()` - получение проекта по имени
- `delete_project()` - удаление проекта
- `set_active_project()` - установка активного проекта
- `get_active_project()` - получение активного проекта
- `get_project_stats()` - статистика по проекту

**handlers/project.py** - обработчики команд для работы с проектами

### Модифицированные модули

**utils/excel.py** - добавлен параметр `project_id` во все функции:
- `get_excel_path()`
- `create_excel_file()`
- `add_expense()`
- `get_month_expenses()`
- `set_budget()`
- `get_category_expenses()`
- `get_all_expenses()`
- `get_day_expenses()`

**handlers/expense.py** - обработчики добавления расходов учитывают активный проект

**handlers/stats.py** - обработчики статистики учитывают активный проект

**handlers/export.py** - обработчики экспорта учитывают активный проект

**utils/visualization.py** - функции визуализации учитывают активный проект

### Контекст пользователя

В `context.user_data` хранится:
- `active_project_id` - ID активного проекта (None = общие расходы)

Контекст сохраняется между сообщениями в рамках одной сессии.

## Обратная совместимость

Функционал полностью обратно совместим:
- Существующие данные не затрагиваются
- Если проект не выбран, все работает как раньше (общие расходы)
- Все существующие команды продолжают работать

## Ограничения

- Название проекта должно быть уникальным для пользователя
- Удаление проекта необратимо
- Нельзя переносить расходы между проектами
- Категории общие для всех проектов (пока нет кастомных категорий)

## Будущие улучшения

- Возможность переноса расходов между проектами
- Кастомные категории для каждого проекта
- Архивация проектов вместо удаления
- Бюджет для проектов
- Сравнение проектов
- Экспорт всех проектов одним файлом
